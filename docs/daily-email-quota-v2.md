# Daily Email Quota Deployment Note

The outreach worker defaults to 100 successfully sent emails per UTC day. Configure with `DAILY_EMAIL_LIMIT=100` and expose quota status through `GET /api/outreach/quota`.
